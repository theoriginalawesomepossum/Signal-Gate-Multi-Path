<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Core telephony permissions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />

    <!-- Notifications and UI -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Foreground service (Layer 1 — platform boundary) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/shield_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/shield_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignalGate">

        <!-- Single Activity — contract requirement -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Call Screening Service (Layer 1 — system-level call decisions) -->
        <!-- BIND_SCREENING_SERVICE ensures only the system can bind to this service -->
        <service
            android:name=".SignalGateCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="false">

            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>

        </service>

        <!-- Call Overlay Service (Layer 1 — foreground UI during active call) -->
        <service
            android:name=".CallOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

        <!-- Phone state receiver — guards against third-party triggers -->
        <receiver
            android:name=".PhoneStateReceiver"
            android:permission="android.permission.READ_PHONE_STATE"
            android:exported="false">

            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>

        </receiver>

        <!-- Notification action buttons — internal only -->
        <receiver
            android:name=".CallActionReceiver"
            android:exported="false" />

    </application>

</manifest>
